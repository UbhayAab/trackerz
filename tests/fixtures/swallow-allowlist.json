{
  "_": "Generated by `node tests/no-swallowed-errors.test.mjs --seed`. Reasons are hand-written and preserved across regenerations; line/text are refreshed. Delete an entry when you fix the site - the baselines in the test only ratchet down.",
  "entries": [
    {
      "file": "lib/additions.mjs",
      "pattern": "absent-as-zero",
      "line": 107,
      "text": "delta: `${sign}Rs ${INR.format(Number(r.amount || 0))}`,",
      "reason": "ledger_entries.amount is NOT NULL in supabase/schema.sql, so this is a defensive cast on a required column, not a default for absent data. Whether a period was measured at all is decided by row presence one layer up, in period-aggregator/dailySeries."
    },
    {
      "file": "lib/statement-link.mjs",
      "pattern": "absent-as-zero",
      "line": 390,
      "text": "const manualAmount = Number(manual.amount || 0);",
      "reason": "ledger_entries.amount is NOT NULL in supabase/schema.sql, so this is a defensive cast on a required column, not a default for absent data. Whether a period was measured at all is decided by row presence one layer up, in period-aggregator/dailySeries."
    },
    {
      "file": "src/analytics/opportunity-cost.js",
      "pattern": "absent-as-zero",
      "line": 51,
      "text": "const amt = Number(entry.amount || 0);",
      "reason": "ledger_entries.amount is NOT NULL in supabase/schema.sql, so this is a defensive cast on a required column, not a default for absent data. Whether a period was measured at all is decided by row presence one layer up, in period-aggregator/dailySeries."
    },
    {
      "file": "src/config.js",
      "pattern": "empty-catch",
      "line": 34,
      "text": "} catch {",
      "reason": "Correct: src/config.local.js is an optional gitignored file and its absence is the normal case for every checkout. The failed import IS the answer; the function returns null and the next config source (localStorage, then the baked-in production defaults) is tried."
    },
    {
      "file": "src/domain/diet/meal-templates.js",
      "pattern": "absent-as-zero",
      "line": 45,
      "text": "const byCount = [...inSlot].sort((a, b) => (b.use_count || 0) - (a.use_count || 0));",
      "reason": "use_count is a NOT NULL counter defaulting to 0; the fallback only covers template rows written before the column existed. It is a sort key for picking a repeat meal, never a number rendered to the user."
    },
    {
      "file": "src/domain/diet/meal-templates.js",
      "pattern": "absent-as-zero",
      "line": 45,
      "text": "const byCount = [...inSlot].sort((a, b) => (b.use_count || 0) - (a.use_count || 0));",
      "reason": "use_count is a NOT NULL counter defaulting to 0; the fallback only covers template rows written before the column existed. It is a sort key for picking a repeat meal, never a number rendered to the user."
    },
    {
      "file": "src/domain/diet/restaurant-mode.js",
      "pattern": "absent-as-zero",
      "line": 79,
      "text": "const disc = result.discount || 0;",
      "reason": "A parsed restaurant bill with no discount line genuinely has no discount, so 0 is the measured value rather than a stand-in for a failed read. A mis-parse is caught by the subtotal + tax - discount = total reconciliation on the next line, not by this default."
    },
    {
      "file": "src/domain/money/budget-alerts.js",
      "pattern": "absent-as-zero",
      "line": 54,
      "text": "const spent = items.reduce((acc, r) => acc + Math.abs(Number(r.amount || 0)), 0);",
      "reason": "ledger_entries.amount and budgets.amount are both NOT NULL in supabase/schema.sql - defensive casts on required columns. A budget with no rows yet is handled by the item count above, not by defaulting the amount."
    },
    {
      "file": "src/domain/money/budget-alerts.js",
      "pattern": "absent-as-zero",
      "line": 55,
      "text": "const cap = Number(budget.amount || 0);",
      "reason": "ledger_entries.amount and budgets.amount are both NOT NULL in supabase/schema.sql - defensive casts on required columns. A budget with no rows yet is handled by the item count above, not by defaulting the amount."
    },
    {
      "file": "src/domain/wellness/habit-score.js",
      "pattern": "absent-as-zero",
      "line": 49,
      "text": ".map((m) => Number(m.value || 0));",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/habit-score.js",
      "pattern": "absent-as-zero",
      "line": 59,
      "text": "stepsByDay.set(key, (stepsByDay.get(key) || 0) + Number(m.value || 0));",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/habit-score.js",
      "pattern": "absent-as-zero",
      "line": 77,
      "text": ".reduce((a, l) => a + Math.abs(Number(l.amount || 0)), 0);",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/mood-triggers.js",
      "pattern": "absent-as-zero",
      "line": 92,
      "text": "spendByDay.set(key, (spendByDay.get(key) || 0) + Math.abs(Number(l.amount || 0)));",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/sleep-debt.js",
      "pattern": "absent-as-zero",
      "line": 23,
      "text": "perDay.set(key, Number(m.value || 0));",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/step-summary.js",
      "pattern": "absent-as-zero",
      "line": 22,
      "text": "perDay.set(key, (perDay.get(key) || 0) + Number(m.value || 0));",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/weekly-review.js",
      "pattern": "absent-as-zero",
      "line": 36,
      "text": "const totalSpent = expenses.reduce((a, l) => a + Math.abs(Number(l.amount || 0)), 0);",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/weekly-review.js",
      "pattern": "absent-as-zero",
      "line": 37,
      "text": "const totalIncome = income.reduce((a, l) => a + Math.abs(Number(l.amount || 0)), 0);",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/domain/wellness/weekly-review.js",
      "pattern": "absent-as-zero",
      "line": 41,
      "text": "byMerchant.set(key, (byMerchant.get(key) || 0) + Math.abs(Number(l.amount || 0)));",
      "reason": "body_metrics.value and ledger_entries.amount are both NOT NULL columns in supabase/schema.sql, so these are defensive casts on required fields rather than stand-ins for a missing measurement. Absent-versus-measured is decided by whether a row exists for the day, not by this expression."
    },
    {
      "file": "src/duplicates/dedupe-matrix.js",
      "pattern": "absent-as-zero",
      "line": 38,
      "text": "const amtA = Math.abs(Number(a.amount || 0));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/dedupe-matrix.js",
      "pattern": "absent-as-zero",
      "line": 39,
      "text": "const amtB = Math.abs(Number(b.amount || 0));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/dedupe-matrix.js",
      "pattern": "absent-as-zero",
      "line": 107,
      "text": "const parentAmt = Math.abs(Number(parent.amount || 0));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/dedupe-matrix.js",
      "pattern": "absent-as-zero",
      "line": 111,
      "text": "const sums = subsetsSum(sameMerchant.map((c) => Math.abs(Number(c.amount || 0))));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 50,
      "text": "const byAmountDesc = [...rows].sort((x, y) => Math.abs(Number(y?.amount || 0)) - Math.abs(Number(x?.amount || 0)));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 50,
      "text": "const byAmountDesc = [...rows].sort((x, y) => Math.abs(Number(y?.amount || 0)) - Math.abs(Number(x?.amount || 0)));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 53,
      "text": "const parentAmt = Math.abs(Number(parent.amount || 0));",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 60,
      "text": "&& Math.abs(Number(child.amount || 0)) > 0",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 61,
      "text": "&& Math.abs(Number(child.amount || 0)) < parentAmt",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 96,
      "text": ".sort((x, y) => Math.abs(Number(y.amount || 0)) - Math.abs(Number(x.amount || 0)))",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 96,
      "text": ".sort((x, y) => Math.abs(Number(y.amount || 0)) - Math.abs(Number(x.amount || 0)))",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/expense-duplicates.js",
      "pattern": "absent-as-zero",
      "line": 103,
      "text": "if (mask & (1 << i)) { items.push(pool[i]); total += Math.abs(Number(pool[i].amount || 0)); }",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/score-pair.js",
      "pattern": "absent-as-zero",
      "line": 24,
      "text": "const amtA = Number(a.amount || 0);",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/duplicates/score-pair.js",
      "pattern": "absent-as-zero",
      "line": 25,
      "text": "const amtB = Number(b.amount || 0);",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/pages/capture.js",
      "pattern": "empty-catch",
      "line": 77,
      "text": "} catch { /* briefing is a nudge, never block the page */ }",
      "reason": "Correct: the briefing strip is a proactive nudge layered on top of the capture page. Capture is the one thing this page must always do, and a missing nudge is visibly missing - it is not a number the user could mistake for measured."
    },
    {
      "file": "src/pages/quick-log.js",
      "pattern": "catch-empty-obj",
      "line": 223,
      "text": "if (flushing) await flushing.catch(() => {});",
      "reason": "Correct: this awaits an in-flight water flush purely to serialise the next batch behind it. The flush reports its own outcome at its own call site, so re-reporting the same rejection here would double-count one failure."
    },
    {
      "file": "src/pages/quick-log.js",
      "pattern": "empty-catch",
      "line": 481,
      "text": "} catch { /* non-browser or opaque origin */ }",
      "reason": "Correct: rewriting the URL after a one-tap water log is idempotency housekeeping so a reload cannot pour a second glass. history.replaceState is unavailable on an opaque origin and in non-browser contexts; the tap itself has already been counted server-side."
    },
    {
      "file": "src/services/audio-convert.js",
      "pattern": "empty-catch",
      "line": 141,
      "text": "try { await ctx?.close(); } catch { /* already closed */ }",
      "reason": "Closing an AudioContext that is already closed throws, and there is nothing a user could do about it. This runs in a finally whose whole job is to release the decoder after the real error has ALREADY been reported by the catch above it - rethrowing here would replace a useful message ('could not convert to wav') with a meaningless one."
    },
    {
      "file": "src/services/auth.js",
      "pattern": "empty-catch",
      "line": 67,
      "text": "try { globalThis.localStorage?.removeItem(LOCAL_SESSION_KEY); } catch { /* private mode */ }",
      "reason": "Correct: sign-out and local-session teardown must always clear the client, whether localStorage is blocked (private mode), there is no browser at all (Node/tests), or the network is down. Leaving a user apparently signed in because the revocation call failed is strictly worse than losing the server-side revocation, which the expiring JWT handles anyway."
    },
    {
      "file": "src/services/auth.js",
      "pattern": "empty-catch",
      "line": 113,
      "text": "} catch { /* non-browser */ }",
      "reason": "Correct: sign-out and local-session teardown must always clear the client, whether localStorage is blocked (private mode), there is no browser at all (Node/tests), or the network is down. Leaving a user apparently signed in because the revocation call failed is strictly worse than losing the server-side revocation, which the expiring JWT handles anyway."
    },
    {
      "file": "src/services/auth.js",
      "pattern": "empty-catch",
      "line": 264,
      "text": "} catch {",
      "reason": "Correct: sign-out and local-session teardown must always clear the client, whether localStorage is blocked (private mode), there is no browser at all (Node/tests), or the network is down. Leaving a user apparently signed in because the revocation call failed is strictly worse than losing the server-side revocation, which the expiring JWT handles anyway."
    },
    {
      "file": "src/services/dedupe-scan.js",
      "pattern": "absent-as-zero",
      "line": 37,
      "text": "const amtA = Number(a.amount || 0);",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/services/dedupe-scan.js",
      "pattern": "absent-as-zero",
      "line": 38,
      "text": "const amtB = Number(b.amount || 0);",
      "reason": "Duplicate scoring compares two rows that already exist in ledger_entries, where amount is NOT NULL. A zero here can only come from a genuinely zero-amount row, which just fails the amount-tolerance match instead of producing a fabricated number."
    },
    {
      "file": "src/services/health-sync.js",
      "pattern": "empty-catch",
      "line": 384,
      "text": "try { globalThis.localStorage?.setItem(AUTO_SYNC_KEY, String(Date.now())); } catch { /* ignore */ }",
      "reason": "Correct: this stamps the last auto-sync time in localStorage purely to throttle the next Health Connect pull to once per 6h. A blocked write costs one extra sync attempt; the sync result itself is returned and rendered from the return value."
    },
    {
      "file": "src/services/latency-stats.js",
      "pattern": "empty-catch",
      "line": 38,
      "text": "} catch {",
      "reason": "Correct: a full or disabled localStorage must not break a capture. The latency curve stops learning and the seeded estimate keeps the progress bar honest - nothing the user reads becomes wrong."
    },
    {
      "file": "src/services/notification-capture.js",
      "pattern": "empty-catch",
      "line": 55,
      "text": "} catch { /* storage unavailable - falls back to the server fingerprint */ }",
      "reason": "Correct: the localStorage-backed idempotency cache and enabled flag both degrade to the server-side capture fingerprint, which is the real duplicate guard. Storage being unavailable makes the client do more round trips, not produce wrong data."
    },
    {
      "file": "src/services/notification-capture.js",
      "pattern": "empty-catch",
      "line": 62,
      "text": "try { globalThis.localStorage?.setItem(ENABLED_KEY, on ? \"1\" : \"0\"); } catch { /* ignore */ }",
      "reason": "Correct: the localStorage-backed idempotency cache and enabled flag both degrade to the server-side capture fingerprint, which is the real duplicate guard. Storage being unavailable makes the client do more round trips, not produce wrong data."
    },
    {
      "file": "src/services/notification-capture.js",
      "pattern": "empty-catch",
      "line": 126,
      "text": "} catch { /* storage unavailable - the server fingerprint is the backstop */ }",
      "reason": "Correct: the localStorage-backed idempotency cache and enabled flag both degrade to the server-side capture fingerprint, which is the real duplicate guard. Storage being unavailable makes the client do more round trips, not produce wrong data."
    },
    {
      "file": "src/services/privacy-mode.js",
      "pattern": "empty-catch",
      "line": 21,
      "text": "} catch {",
      "reason": "Correct: privacy mode is re-applied from the body class on every render, so a failed localStorage write costs the preference at next load and nothing else. No number on screen depends on it."
    },
    {
      "file": "src/services/pwa.js",
      "pattern": "catch-null",
      "line": 122,
      "text": "const flush = () => drainOfflineQueue(runCapture).catch(() => null);",
      "reason": "REAL GAP, DEFERRED: a failed offline-queue drain is invisible here, so captures can sit unsent in the outbox while the UI shows nothing wrong - the same shape as the dedupe pipeline that went dark for weeks. Fix is to surface the drain result; src/services/pwa.js is owned by another workstream in this phase."
    },
    {
      "file": "src/services/sms-capture.js",
      "pattern": "empty-catch",
      "line": 73,
      "text": "} catch { /* storage full / unavailable - idempotency degrades to the server fingerprint */ }",
      "reason": "Correct: storage-backed idempotency degrades to the server fingerprint, and a single unparseable SMS must not tear down the listener for every later message. Each message's parse outcome is reported separately by the capture pipeline."
    },
    {
      "file": "src/services/sms-capture.js",
      "pattern": "empty-catch",
      "line": 82,
      "text": "try { globalThis.localStorage?.setItem(ENABLED_KEY, on ? \"1\" : \"0\"); } catch { /* ignore */ }",
      "reason": "Correct: storage-backed idempotency degrades to the server fingerprint, and a single unparseable SMS must not tear down the listener for every later message. Each message's parse outcome is reported separately by the capture pipeline."
    },
    {
      "file": "src/services/sms-capture.js",
      "pattern": "empty-catch",
      "line": 193,
      "text": "} catch { /* one bad SMS must not kill the listener */ }",
      "reason": "Correct: storage-backed idempotency degrades to the server fingerprint, and a single unparseable SMS must not tear down the listener for every later message. Each message's parse outcome is reported separately by the capture pipeline."
    },
    {
      "file": "src/services/sms-capture.js",
      "pattern": "empty-catch",
      "line": 196,
      "text": "return () => { try { handle.remove(); } catch { /* ignore */ } liveHandle = null; };",
      "reason": "Correct: storage-backed idempotency degrades to the server fingerprint, and a single unparseable SMS must not tear down the listener for every later message. Each message's parse outcome is reported separately by the capture pipeline."
    },
    {
      "file": "src/services/speech.js",
      "pattern": "empty-catch",
      "line": 253,
      "text": "watchdog = setTimeout(() => { try { rec?.abort(); } catch { /* already dead */ } }, WATCHDOG_MS);",
      "reason": "Stopping a recogniser that has already stopped throws, and there is nothing the user could do about it. This is the back-compat shim's stop(): the transcript is already committed by the time it runs, so rethrowing would replace a completed dictation with an error about cleanup."
    },
    {
      "file": "src/services/speech.js",
      "pattern": "empty-catch",
      "line": 339,
      "text": "stop() { userStopped = true; clearTimeout(watchdog); try { rec?.stop(); } catch { /* already stopped */ } },",
      "reason": "Stopping a recogniser that has already stopped throws, and there is nothing the user could do about it. This is the back-compat shim's stop(): the transcript is already committed by the time it runs, so rethrowing would replace a completed dictation with an error about cleanup."
    },
    {
      "file": "src/services/speech.js",
      "pattern": "empty-catch",
      "line": 341,
      "text": "abort() { userStopped = true; clearTimeout(watchdog); try { rec?.abort(); } catch { /* already stopped */ } },",
      "reason": "Stopping a recogniser that has already stopped throws, and there is nothing the user could do about it. This is the back-compat shim's stop(): the transcript is already committed by the time it runs, so rethrowing would replace a completed dictation with an error about cleanup."
    },
    {
      "file": "src/services/speech.js",
      "pattern": "empty-catch",
      "line": 365,
      "text": "try { rec?.stop?.(); } catch { /* already stopped */ }",
      "reason": "Stopping a recogniser that has already stopped throws, and there is nothing the user could do about it. This is the back-compat shim's stop(): the transcript is already committed by the time it runs, so rethrowing would replace a completed dictation with an error about cleanup."
    },
    {
      "file": "src/services/statement-import.js",
      "pattern": "empty-catch",
      "line": 199,
      "text": "} catch {",
      "reason": "Correct: this only stamps the import row's status AFTER the ledger rows are written. Failing to record 'promoted' loses no data, and every real failure above it is already pushed into report.warnings, which the importer shows the user."
    },
    {
      "file": "src/services/supabase-data.js",
      "pattern": "absent-as-zero",
      "line": 725,
      "text": ".update({ use_count: (template.use_count || 0) + 1, last_used_at: new Date().toISOString() })",
      "reason": "use_count is a NOT NULL counter with a default of 0; the fallback only covers meal-template rows written before the column existed. It is incremented, never displayed as a measurement."
    },
    {
      "file": "src/services/supabase-data.js",
      "pattern": "empty-catch",
      "line": 1470,
      "text": "} catch {",
      "reason": "Correct and deliberate: storage-object cleanup is best-effort during delete-all-my-data. Row deletion is the source of truth and its failures are collected into the returned `errors` array; an orphaned media file is not user-visible data."
    },
    {
      "file": "src/services/undo-service.js",
      "pattern": "empty-catch",
      "line": 207,
      "text": "try { await markReverted(supabase, step.actionId, step.payload); } catch { /* status is cosmetic here */ }",
      "reason": "Correct: this is the alreadyUndone branch, where the row is already gone and marking the ai_action reverted is bookkeeping on a fact that is no longer in doubt. The caller still gets the step back in `alreadyUndone`. Note the branch directly above deliberately does NOT swallow the same call - there, a failed mark leaves an action claiming a row that no longer exists, and it is reported as undone_but_unmarked."
    },
    {
      "file": "src/state/app-state.js",
      "pattern": "empty-catch",
      "line": 90,
      "text": "} catch {",
      "reason": "Correct: local workspace persistence is a convenience. State is rebuilt from Supabase by hydrateStateFromSupabase() on every load, so a blocked localStorage costs offline convenience and never a number."
    },
    {
      "file": "src/state/sync.js",
      "pattern": "catch-empty-obj",
      "line": 109,
      "text": "await persistDetectedSubscriptions(detectedSubs).catch(() => {});",
      "reason": "REAL GAP, DEFERRED: a failed persistDetectedSubscriptions() means the detected subscription set silently reverts to the last saved one on the next load. This render still uses the freshly detected list, so numbers are not wrong now, only unsaved - and the user is never told. src/state/sync.js is owned by another workstream in this phase."
    },
    {
      "file": "src/state/sync.js",
      "pattern": "absent-as-zero",
      "line": 154,
      "text": "rows: String(row.row_count ?? 0),",
      "reason": "Mostly defensive casts on NOT NULL ledger_entries.amount. The exception is `row_count ?? 0`, which renders an import whose row count was never recorded as '0 rows' - a real if small instance of the disease, in a status cell on the imports table rather than a total the user acts on. src/state/sync.js is owned by another workstream in this phase."
    },
    {
      "file": "src/state/sync.js",
      "pattern": "absent-as-zero",
      "line": 170,
      "text": ".reduce((acc, r) => acc + Number(r.amount || 0), 0);",
      "reason": "Mostly defensive casts on NOT NULL ledger_entries.amount. The exception is `row_count ?? 0`, which renders an import whose row count was never recorded as '0 rows' - a real if small instance of the disease, in a status cell on the imports table rather than a total the user acts on. src/state/sync.js is owned by another workstream in this phase."
    },
    {
      "file": "src/state/sync.js",
      "pattern": "absent-as-zero",
      "line": 319,
      "text": ".reduce((acc, r) => acc + Number(r.amount || 0), 0);",
      "reason": "Mostly defensive casts on NOT NULL ledger_entries.amount. The exception is `row_count ?? 0`, which renders an import whose row count was never recorded as '0 rows' - a real if small instance of the disease, in a status cell on the imports table rather than a total the user acts on. src/state/sync.js is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/auth-gate.js",
      "pattern": "empty-catch",
      "line": 304,
      "text": "try { history.replaceState(null, \"\", location.pathname); } catch { /* non-browser */ }",
      "reason": "Correct: history.replaceState here is cosmetic URL cleanup after an OAuth redirect, and is unavailable in non-browser contexts. The sign-in flow has already consumed the token fragment; failing this leaves an ugly address bar and nothing else."
    },
    {
      "file": "src/ui/briefing-strip.js",
      "pattern": "catch-empty-obj",
      "line": 58,
      "text": "if (briefing.id) markBriefingSeen(briefing.id).catch(() => {});",
      "reason": "REAL GAP, DEFERRED: dismissing a briefing hides the strip locally whether or not markBriefingSeen() succeeded, so the same brief reappears on the next load with no explanation. Cosmetic rather than numeric, but the same shape. src/ui/briefing-strip.js is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/capture-panel.js",
      "pattern": "catch-null",
      "line": 265,
      "text": "if (outboxId) await markSending(outboxId).catch(() => null);",
      "reason": "MIXED, DEFERRED: audioCtx.close() is genuinely correct (closing an already-closed AudioContext has nothing to report), but markSending/markSent/markFailed swallowing leaves an outbox row stuck in the wrong state with no signal, which is how a capture can look sent and never be. src/ui/capture-panel.js is explicitly out of scope for this phase."
    },
    {
      "file": "src/ui/capture-panel.js",
      "pattern": "catch-null",
      "line": 324,
      "text": "if (outboxId) await markSent(outboxId, result?.ingestion?.id || null).catch(() => null);",
      "reason": "MIXED, DEFERRED: audioCtx.close() is genuinely correct (closing an already-closed AudioContext has nothing to report), but markSending/markSent/markFailed swallowing leaves an outbox row stuck in the wrong state with no signal, which is how a capture can look sent and never be. src/ui/capture-panel.js is explicitly out of scope for this phase."
    },
    {
      "file": "src/ui/capture-panel.js",
      "pattern": "catch-null",
      "line": 346,
      "text": "if (outboxId) outcome = await markFailed(outboxId, err).catch(() => null);",
      "reason": "MIXED, DEFERRED: audioCtx.close() is genuinely correct (closing an already-closed AudioContext has nothing to report), but markSending/markSent/markFailed swallowing leaves an outbox row stuck in the wrong state with no signal, which is how a capture can look sent and never be. src/ui/capture-panel.js is explicitly out of scope for this phase."
    },
    {
      "file": "src/ui/capture-panel.js",
      "pattern": "catch-null",
      "line": 731,
      "text": "audioCtx.close().catch(() => null);",
      "reason": "MIXED, DEFERRED: audioCtx.close() is genuinely correct (closing an already-closed AudioContext has nothing to report), but markSending/markSent/markFailed swallowing leaves an outbox row stuck in the wrong state with no signal, which is how a capture can look sent and never be. src/ui/capture-panel.js is explicitly out of scope for this phase."
    },
    {
      "file": "src/ui/capture-panel.js",
      "pattern": "empty-catch",
      "line": 739,
      "text": "try { waveformHandle?.stop(); } catch {}",
      "reason": "Correct: stop() on a MediaRecorder or SpeechRecognition that has already stopped throws and means nothing - the recorder is already in the state the caller asked for."
    },
    {
      "file": "src/ui/charts.js",
      "pattern": "absent-as-zero",
      "line": 57,
      "text": "const expenseOf = (r) => (r.direction === \"expense\" ? Math.abs(Number(r.amount || 0)) : 0);",
      "reason": "ledger_entries.amount is NOT NULL and the `: 0` beside it is the deliberate 'income rows contribute no spend' branch. The real follow-up in this file is different: dailySeries() now returns { value: null, measured: false } for a day with no rows, and buildTrendData still coerces that gap to a 0-height bar. src/ui/charts.js is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/diet-plan-panel.js",
      "pattern": "empty-catch",
      "line": 80,
      "text": "try { globalThis.localStorage?.setItem(STATE_PREFIX + key, JSON.stringify(state)); } catch { /* private mode */ }",
      "reason": "Correct: per-day plan tick state is mirrored to localStorage as an offline convenience and is reconciled against food_logs on every render. A blocked write in private mode costs the local mirror, not the record."
    },
    {
      "file": "src/ui/health-panel.js",
      "pattern": "empty-catch",
      "line": 42,
      "text": "} catch {",
      "reason": "Correct: the last-sync timestamp is a display convenience written to localStorage. The authoritative sync result comes back from health-sync.js and is rendered from that return value, not from storage."
    },
    {
      "file": "src/ui/jarvis-settings.js",
      "pattern": "empty-catch",
      "line": 67,
      "text": "try { if (state.endpoint) host = new URL(state.endpoint).host; } catch { /* keep the em-dash */ }",
      "reason": "Correct: this parses a Web Push endpoint URL only to show its host in the settings row. An unparseable endpoint keeps the placeholder text; no setting, toggle or number depends on it."
    },
    {
      "file": "src/ui/navigation.js",
      "pattern": "empty-catch",
      "line": 187,
      "text": "} catch { /* best effort */ }",
      "reason": "Correct: this is the hard-refresh escape hatch clearing caches and unregistering service workers. It must reach the cache-busted reload on the next line whatever fails, and the reload is the user-visible outcome the button promises."
    },
    {
      "file": "src/ui/quick-actions.js",
      "pattern": "catch-empty-obj",
      "line": 202,
      "text": "if (_waterFlush) await _waterFlush.catch(() => {});",
      "reason": "Correct: this awaits an in-flight water flush only to serialise the next batch behind it; the flush reports its own outcome at its own call site. src/ui/quick-actions.js is explicitly out of scope for this phase in any case."
    },
    {
      "file": "src/ui/statement-importer.js",
      "pattern": "empty-catch",
      "line": 67,
      "text": "try { fileInput.files = files; } catch { /* read-only in some browsers */ }",
      "reason": "Correct: assigning to fileInput.files is progressive enhancement for drag-and-drop and is read-only in some browsers. The analysis path already holds the File objects it needs."
    },
    {
      "file": "src/ui/theme.js",
      "pattern": "empty-catch",
      "line": 38,
      "text": "try { localStorage.setItem(KEY, pref); } catch { /* private mode */ }",
      "reason": "Correct: a blocked localStorage costs the theme preference at next load, and a browser without matchMedia().addEventListener simply stops following live OS theme changes. The theme itself is applied either way, on the line above."
    },
    {
      "file": "src/ui/theme.js",
      "pattern": "empty-catch",
      "line": 72,
      "text": "} catch { /* older browsers */ }",
      "reason": "Correct: a blocked localStorage costs the theme preference at next load, and a browser without matchMedia().addEventListener simply stops following live OS theme changes. The theme itself is applied either way, on the line above."
    },
    {
      "file": "src/ui/workout-panel.js",
      "pattern": "empty-catch",
      "line": 34,
      "text": "function saveDay(key, s) { try { globalThis.localStorage?.setItem(STATE_PREFIX + key, JSON.stringify(s)); } catch { /* private mode */ } }",
      "reason": "REAL GAP, DEFERRED: same site as the catch-empty-obj entries above - a failed logWorkoutSession/deleteRow leaves the local tick and the server disagreeing silently. src/ui/workout-panel.js is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/workout-panel.js",
      "pattern": "empty-catch",
      "line": 229,
      "text": "} catch { /* keep the local check */ }",
      "reason": "REAL GAP, DEFERRED: same site as the catch-empty-obj entries above - a failed logWorkoutSession/deleteRow leaves the local tick and the server disagreeing silently. src/ui/workout-panel.js is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/workout-panel.js",
      "pattern": "empty-catch",
      "line": 244,
      "text": "try { await deleteRow(\"workout_logs\", prev.recordId); await refreshAfterWrite(\"the set\"); } catch { /* best effort */ }",
      "reason": "REAL GAP, DEFERRED: same site as the catch-empty-obj entries above - a failed logWorkoutSession/deleteRow leaves the local tick and the server disagreeing silently. src/ui/workout-panel.js is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/workout-panel.js",
      "pattern": "empty-catch",
      "line": 284,
      "text": "} catch { /* best effort */ }",
      "reason": "REAL GAP, DEFERRED: same site as the catch-empty-obj entries above - a failed logWorkoutSession/deleteRow leaves the local tick and the server disagreeing silently. src/ui/workout-panel.js is owned by another workstream in this phase."
    }
  ],
  "renderersWithoutErrorBranch": [
    {
      "file": "src/ui/auth-gate.js",
      "reason": "Already surfaces failures, just not with any of the five tokens this test greps for: it renders getLastAuthError() into an .auth-banner and into role=status paragraphs. The follow-up is to route it through lib/failure.mjs, not to add an error branch it has."
    },
    {
      "file": "src/ui/diet-plan-panel.js",
      "reason": "Reads fetchDayLogs(). A failed read renders the plan with nothing ticked, which is indistinguishable from a day the user has not eaten yet. Real gap; the file is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/gym-today.js",
      "reason": "Reads fetchTodayGymAnswer(). A failed read renders the 'did you train today?' prompt as unanswered, which can re-ask a question the user already answered. Real gap; the file is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/jarvis-settings.js",
      "reason": "Reads fetchJarvisProfile(). A failed read renders every Jarvis toggle in its default OFF position, which the user reads as a setting they chose. Real gap; the file is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/meal-chips.js",
      "reason": "Reads fetchFoodLogs(). A failed read renders no repeat-meal chips, identical to a user who has never logged a meal. Real gap; the file is owned by another workstream in this phase."
    },
    {
      "file": "src/ui/quick-actions.js",
      "reason": "Reads fetchHydrationTotal/fetchWaterGoalMl/fetchOpenSleepSession/fetchTodayGymAnswer. A failed hydration read renders '0 ml today', which is the exact fabricated-zero this phase exists to stop. src/ui/quick-actions.js is explicitly out of scope for this phase."
    },
    {
      "file": "src/ui/reminders-panel.js",
      "reason": "STALE AS WRITTEN, kept because the grep still matches. As of the 2026-08-06 calendar work this file DOES surface both of its reads: fetchCalendarReminders() and fetchAgentTasks() fail independently, each keeps its message, and renderCalendar() draws a named failure with a retry instead of an empty calendar (tests/calendar-ui.test.mjs asserts that branch). It stays listed only because it uses none of the five tokens ERROR_BRANCH greps for - the follow-up is to route it through lib/failure.mjs, not to add an error branch it already has."
    },
    {
      "file": "src/ui/spend-chips.js",
      "reason": "Reads fetchSpendPatternHistory(). A failed read renders no suggestion chips, indistinguishable from having no spend history. Real gap; the file is owned by another workstream in this phase."
    }
  ]
}
